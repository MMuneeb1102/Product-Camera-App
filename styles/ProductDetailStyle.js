import { StyleSheet } from "react-native";

export default ProductDetailStyle = StyleSheet.create({
    container: {padding: 16},
    
    image: {
        height: 300,
        resizeMode: 'contain',
    },

    title: {
        frontSize: 20,
        fontWeight: 'bold',
        marginVertical: 10
    }
})